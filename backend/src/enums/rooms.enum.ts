export enum RoomStatus {
  AVAILABLE = "available",
  BOOKED = "booked",
  CLEANING = "cleaning",
  MAINTENANCE = "maintenance",
}

export enum RoomType {
  FAMILY = "family",
  DELUXE = "deluxe",
  STANDARD = "standard",
}
